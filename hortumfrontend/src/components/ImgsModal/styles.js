import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container_imgs:{
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: "space-between",

    },

    img:{
        width: 5,
        height: 5,
        marginBottom: 10
    }
});